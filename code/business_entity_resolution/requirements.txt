# Core
pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.3.0
scipy>=1.10.0
networkx>=3.1
rapidfuzz>=3.5.0
tqdm>=4.66.0

# Optional (Innovation 3: GAT ablation). Pipeline runs fully without these;
# graph_reasoning.py falls back to a no-op if torch is unavailable.
# torch>=2.1.0

# Testing
pytest>=7.4.0
